# name: Build and Deploy to cPanel

# on:
#   push:
#     branches:
#       - enhancements  # Changed to match your current branch
#   workflow_dispatch:  # Allows manual trigger

# jobs:
#   build-and-deploy:
#     runs-on: ubuntu-latest
    
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20.18.3'
#           cache: 'npm'
#           cache-dependency-path: |
#             package-lock.json
#             frontend/package-lock.json

#       - name: Install backend dependencies
#         run: npm ci

#       - name: Install frontend dependencies
#         run: |
#           cd frontend
#           npm ci

#       - name: Build frontend
#         run: |
#           cd frontend
#           npm run build
#         env:
#           NODE_ENV: production

#       - name: Prepare deployment package
#         run: |
#           mkdir -p deploy
#           # Copy backend files
#           cp -r backend deploy/
#           # Move frontend/dist to backend/frontend/
#           mkdir -p deploy/backend/frontend
#           cp -r frontend/dist deploy/backend/frontend/
#           # Remove backend/frontend/dist if it exists
#           rm -rf deploy/backend/frontend/dist
#           # Copy package files
#           cp package.json package-lock.json deploy/
          
#       - name: Deploy to cPanel via FTP
#         uses: SamKirkland/FTP-Deploy-Action@v4.3.4
#         with:
#           server: ${{ secrets.FTP_SERVER }}
#           username: ${{ secrets.FTP_USERNAME }}
#           password: ${{ secrets.FTP_PASSWORD }}
#           local-dir: ./deploy/
#           server-dir: /home/hrlabels/
#           exclude: |
#             **/.git*
#             **/.git*/**
#             **/node_modules/**
#             **/.env*
#             **/database/**

#       - name: Install dependencies on server
#         uses: appleboy/ssh-action@v1.0.0
#         with:
#           host: ${{ secrets.FTP_SERVER }}
#           username: ${{ secrets.SSH_USERNAME }}
#           password: ${{ secrets.SSH_PASSWORD }}
#           port: ${{ secrets.SSH_PORT }}
          